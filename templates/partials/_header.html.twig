<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm py-3 mb-">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('app_product_index') }}">Produits</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_product_index') }}">
                        Produits
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav">

                {% if app.user %}

                    <li class="nav-item">
                        <span class="nav-link">
                            Bonjour {{ app.user.email }}
                        </span>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link btn btn-danger text-white ms-2 bg-danger"
                        href="{{ path('app_logout') }}">
                            Déconnexion
                        </a>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link btn btn-primary text-white me-2 bg-primary"
                        href="{{ path('app_login') }}">
                            Connexion
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link btn btn-success text-white bg-success"
                        href="{{ path('app_register') }}">
                            Inscription
                        </a>
                    </li>

                {% endif %}

            </ul>

        </div>
    </div>
</nav>